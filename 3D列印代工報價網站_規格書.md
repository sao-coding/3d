# 3D 列印代工報價計算機 — 網站規格書 / AI 提示詞

> 本文件可直接複製貼上給 Claude Code、v0.dev、Bolt、Cursor 等 AI 開發工具，
> 用來產生一個完整可用的報價計算網站（含後台管理）。

---

## 一、專案概述

**目的**：建立一個給自己使用的「熟人/同事 3D 列印代工報價計算機」，
輸入列印參數後自動算出建議報價，並有後台管理自己購入的線材資料。

**使用情境**：
1. 朋友傳訊息問「印這個多少錢」時，快速打開網頁選擇線材、輸入列印參數，馬上得到報價
2. 買新線材時，到後台登記價格、顏色、廠牌、備註說明，之後報價時直接從清單選取

**使用者**：僅自己一人使用（單一使用者系統），需要登入保護後台管理功能，
避免公開網址被其他人看到/竄改你的成本資料。

---

## 二、技術規格（已確定）

| 項目 | 選用技術 |
|---|---|
| 前端框架 | React + **TanStack Start**（full-stack React framework，含 SSR 與檔案式路由） |
| 身份驗證 | **Better Auth**（email/password 登入即可，單一使用者，不需要註冊開放） |
| API 層 | **oRPC**（type-safe RPC，前後端共用型別，不用手動維護 API 型別） |
| 資料庫 | **SQLite**（透過 Drizzle ORM），單一使用者情境不需要 Postgres 等級的資料庫，部署簡單、備份方便（就是一個檔案） |
| ORM | **Drizzle ORM**（跟 Better Auth、oRPC 生態系整合度高，型別安全） |
| 貨幣符號 | 全站一律使用 **$**（台灣使用習慣，數字前綴，例如 $358） |
| 語言 | 繁體中文（台灣用語） |

---

## 三、資料庫 Schema

### 1. `filaments`（線材清單，後台管理的核心資料表）
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | text (uuid) | 主鍵 |
| name | text | 線材名稱（例如：JP PETG-Basic 消光黑） |
| brand | text | 廠牌 |
| material_type | text | 材質類型：PLA / PETG / ABS / PC / Nylon / TPU / 其他，用於自動判斷電費檔位（低溫/高溫） |
| color | text | 顏色（可存色票 hex 碼方便顯示色塊，例如 #1a1a1a） |
| purchase_price | integer | 購入價（元） |
| weight_grams | integer | 該捲淨重（克），預設 1000 |
| cost_per_gram | real | 計算欄位：purchase_price ÷ weight_grams（可存成 generated column 或後端算好存入） |
| description | text | 敘述說明（例如：這捲偏軟、適合結構件、朋友反應這色很漂亮等備註） |
| is_active | boolean | 是否還在使用中（用完的線材可標記 false，但保留歷史紀錄不刪除，方便查以前用什麼料報過價） |
| purchased_at | datetime | 購入日期 |
| created_at / updated_at | datetime | 系統自動紀錄 |

### 2. `pricing_settings`（單一使用者的全域參數設定，只會有1筆資料）
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | text | 固定值即可（單一設定列） |
| failure_rate_percent | integer | 預設失敗率攤提%，預設12 |
| depreciation_per_hour | integer | 每小時折舊費，預設5 |
| electricity_summer | real | 夏季每度電價，預設6.0 |
| electricity_offseason | real | 非夏季每度電價，預設4.8 |
| slicing_fixed_fee | integer | 切片開機固定費，預設30 |
| cleanup_rate_per_minute | integer | 清理人工每分鐘費用，預設3 |
| revision_fee | integer | 超額修改每版費用，預設250 |
| free_revision_count | integer | 免費修改版本數，預設2（第1版含在建模費、第2版免費） |
| updated_at | datetime | |

### 3. `modeling_tiers`（建模複雜度分級，可在後台調整每級預設金額）
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | text | 主鍵 |
| tier_name | text | 例如：有範本可改 / 簡單 / 中等 / 複雜 |
| default_price | integer | 該級預設建模費 |
| sort_order | integer | 顯示順序 |

### 4. `quotes`（報價歷史紀錄，方便之後查「上次收這個朋友多少」）
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | text (uuid) | 主鍵 |
| recipient_name | text | 對象名稱（可選填，例如：同事小陳） |
| filament_id | text | 關聯 filaments.id |
| weight_grams | real | 本次列印克數 |
| print_hours | real | 列印小時數 |
| season | text | summer / offseason |
| cleanup_minutes | integer | 清理花費分鐘數 |
| failure_rate_percent | integer | 本次採用的失敗率% |
| needs_modeling | boolean | 是否含建模 |
| modeling_tier_id | text | 關聯 modeling_tiers.id（可為空） |
| modeling_custom_price | integer | 若手動輸入建模費則用這欄 |
| revision_count | integer | 總共修改版本數 |
| material_cost | integer | 計算結果快照 |
| electricity_depreciation_cost | integer | 計算結果快照 |
| labor_cost | integer | 計算結果快照 |
| failure_buffer_cost | integer | 計算結果快照 |
| modeling_cost | integer | 計算結果快照 |
| revision_cost | integer | 計算結果快照 |
| total_price | integer | 總價 |
| rounded_price | integer | 湊整數後的建議收費 |
| created_at | datetime | |

### 5. Better Auth 相關資料表
Better Auth 會自動建立/管理 `user`、`session`、`account`、`verification` 這幾張表，
不需要手動設計，依照 Better Auth 官方文件搭配 Drizzle adapter 產生即可。

---

## 四、核心計算邏輯（前端即時運算 + oRPC 後端可重算驗證）

### 總公式
```
總價 = 建模費 + 超額修改版本費 + 材料費 + 電費 + 折舊費 + 代印人工費 + 失敗率攤提
```

### 1. 材料費
```
每克成本 = 選定線材的 purchase_price ÷ weight_grams（後台已算好存在 filaments.cost_per_gram）
材料費 = 本次列印克數 × 每克成本
```

### 2. 電費（依 filaments.material_type 自動判斷檔位）
```
低溫材質（PLA / PETG / TPU）：
  夏季：0.24度/小時 × electricity_summer(預設6.0) = 每小時約1.44元
  非夏季：0.24度/小時 × electricity_offseason(預設4.8) = 每小時約1.15元

高溫材質（ABS / PC / Nylon，需主動腔體加熱）：
  抓 0.4~0.5度/小時 × 對應電價
```
- 系統依當天日期自動判斷夏季(6-9月)/非夏季，但頁面提供手動切換開關

### 3. 折舊費
```
折舊費 = 列印小時數 × depreciation_per_hour（預設5元）
```

### 4. 代印人工費
```
代印人工費 = slicing_fixed_fee（預設30） + cleanup_minutes × cleanup_rate_per_minute（預設3）
```

### 5. 失敗率攤提
```
失敗率攤提 = (材料費 + 電費 + 折舊費) × failure_rate_percent%
```

### 6. 建模費
```
若 needs_modeling = true：
  建模費 = 選定 modeling_tiers.default_price，或手動輸入 modeling_custom_price
```

### 7. 修改版本費
```
超額修改版本數 = max(0, revision_count - free_revision_count)
超額修改版本費 = 超額修改版本數 × revision_fee
```

---

## 五、頁面架構

### A. 前台（登入後才能使用，因為涉及成本資料）

**首頁 `/`：報價計算機**
- 區塊1：線材選擇（下拉選單，從資料庫抓 is_active=true 的線材，顯示名稱+顏色色塊+每克成本）
- 區塊2：列印資訊（材質類型自動帶出、列印時數、季節、失敗率%滑桿、清理分鐘數）
- 區塊3：建模（開關切換顯示，複雜度下拉選單、修改版本數輸入）
- 區塊4：報價結果（逐項列出 + 總價 + 湊整數建議收費 + 複製報價文字按鈕 + 「儲存這筆報價」按鈕存進 quotes 表）

**`/history`：報價歷史**
- 列表顯示過去存過的報價（對象、日期、金額），可搜尋/篩選
- 點進去可看當時完整明細

### B. 後台管理

**`/admin/filaments`：線材管理**
- 列表顯示所有線材（名稱、顏色色塊、材質類型、每克成本、是否啟用）
- 新增/編輯線材表單：名稱、廠牌、材質類型、顏色（含色票選擇器）、購入價、淨重、敘述說明、購入日期
- 可切換啟用/停用（不刪除歷史資料，因為 quotes 表會關聯到 filament_id）
- 可刪除（若該線材從未被任何報價使用過才允許真刪除，否則只能停用）

**`/admin/settings`：全域參數設定**
- 編輯 pricing_settings 的所有欄位（失敗率預設值、電價、折舊費率、人工費率等）
- 編輯 modeling_tiers 各級距的預設金額

**`/login`：登入頁**
- Better Auth 的 email + password 登入表單
- 不開放註冊（單一使用者帳號直接在資料庫或啟動腳本建立即可）

---

## 六、API 設計（oRPC Router 結構）

```
router
├── filament
│   ├── list()            — 取得所有線材（含停用的，後台用）
│   ├── listActive()       — 只取得啟用中的線材（前台報價下拉選單用）
│   ├── create(input)      — 新增線材
│   ├── update(id, input)  — 編輯線材
│   ├── toggleActive(id)   — 切換啟用狀態
│   └── delete(id)         — 刪除（需檢查是否被 quotes 引用）
│
├── settings
│   ├── get()              — 取得 pricing_settings
│   ├── update(input)      — 更新全域參數
│   ├── listModelingTiers()
│   └── updateModelingTier(id, input)
│
├── quote
│   ├── calculate(input)   — 純計算，不寫入資料庫，前端即時運算也可以直接呼叫這支確保前後端邏輯一致
│   ├── save(input)        — 儲存一筆報價紀錄
│   ├── list()             — 報價歷史列表
│   └── getById(id)
│
└── auth（由 Better Auth 提供，不需自己刻）
```

**重點**：`quote.calculate` 這支 procedure 建議把所有計算公式寫在**後端**（單一真相來源），
前端呼叫這支拿到即時結果並顯示，避免前端 JS 跟後端各寫一份公式，之後改公式要改兩個地方。

---

## 七、視覺與互動風格

- 手機優先（Mobile-first），因為使用情境是朋友傳訊息當下馬上查
- 表單變動後即時呼叫 `quote.calculate`（debounce 300ms 避免打太多次 API）
- 使用清楚的分區卡片（card）排版
- 深色/淺色模式跟隨系統設定
- 全站貨幣一律顯示 `$358` 這種格式（不加小數點，四捨五入到整數元）
- 線材下拉選單建議做成有顏色色塊的自訂 select（不是純文字），方便一眼認出料

---

## 八、部署建議

- SQLite 檔案 + TanStack Start 可以直接部署在單一 VPS 或你自己的 Proxmox homelab 環境上
  （容器化：一個 Node.js container 掛載 volume 存放 sqlite 檔案即可）
- 記得定期備份 sqlite 檔案（linecare線材成本資料是唯一真相來源，遺失就要重新登打）

---

## 九、未來可擴充功能（先不做，僅記錄想法）

- 多盤次加總（一次多個列印任務，自動加總克數與時數再統一算一次報價）
- 針對「同事」vs「朋友」在 quotes 表多存一個 relationship_type 欄位，套用不同預設失敗率/修改費
- 線材庫存量追蹤（每次報價完自動扣掉該線材剩餘克數，庫存低於閾值時提醒要補貨）
- 匯出報價成一張簡易 PDF/圖片方便直接傳給朋友
